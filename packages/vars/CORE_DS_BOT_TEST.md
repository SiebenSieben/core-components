# Core DS bot workflow test

This file is intentionally small and is used to verify that `core-ds-bot` can:

- push a branch to the fork;
- open a pull request in the fork;
- publish simple review comments on that pull request.

It is safe to remove this file after the workflow check is complete.
